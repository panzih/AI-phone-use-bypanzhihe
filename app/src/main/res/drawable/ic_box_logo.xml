<?xml version="1.0" encoding="utf-8"?>
<!--
  「纸盒」图标 —— 一个立方体的线框。

  这是整个应用唯一的 logo，三处都用它：
    1. 启动图标（自适应图标的前景，见 mipmap-anydpi-v26/ic_launcher.xml）
    2. 主界面正中央
    3. 侧边栏标题旁边

  只画一份几何，是为了避免"改了图标但启动图标没跟着变"这种问题。
  应用内用的时候走 Compose 的 Icon(tint = ...)，tint 会整体替换颜色。

  ## 投影方式：斜二测，正面朝前

  之前用的是等轴测（三个面都是菱形，棱交于中心一点）——
  那是骰子的画法，透视关系不对。

  现在按手稿改成正面投影：
    · 正面是一个**正方形**，正对着观察者
    · 进深朝**右上方**收（偏移向量 (+26, -26)）
    · 只看得到三个面：正面、顶面、右侧面

  手稿上就是这么画的，这个画法比方盒子更像"盒子"。

  ## 顶点坐标（画布 108x108）

    正面（正方形）   左下(20,88) 左上(20,46) 右上(62,46) 右下(62,88)
    进深偏移         (+26, -26)
    背面（只露三个）  左上(46,20) 右上(88,20) 右下(88,62)

  可见轮廓是一个六边形，里面三条是棱：
    轮廓  (20,88) → (20,46) → (46,20) → (88,20) → (88,62) → (62,88) → 闭合
    内棱  (20,46)→(62,46)   正面顶边，分隔顶面与正面
          (62,46)→(62,88)   正面右边，分隔正面与右侧面
          (62,46)→(88,20)   分隔顶面与右侧面

  内容范围 20..88，落在自适应图标安全区（18..90）之内。
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <path
        android:fillColor="#00000000"
        android:strokeColor="#6B4F2A"
        android:strokeWidth="3.4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M20,88 L20,46 L46,20 L88,20 L88,62 L62,88 Z
                          M20,46 L62,46 L62,88
                          M62,46 L88,20" />
</vector>
