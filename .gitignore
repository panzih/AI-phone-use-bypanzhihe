# ============================================================
# 纸盒 —— Android 工程
# ============================================================

# ---------- 内部资料：不上传 ----------
# 设计手稿、设计笔记、规划、参考资料都在这里，详见 else/README.md。
# ⚠️ 这里只能保证"以后"不进来。历史上提交过的东西仍然在 git log 里，
# 要彻底不公开得重做一份干净历史（else/README.md 里有说明）。
else/

# ---------- 构建产物（可重建） ----------
build/
app/build/
.gradle/
.kotlin/
captures/
.externalNativeBuild/
.cxx/

# 工程内的 Gradle 缓存 —— **正常情况下不该存在这个目录**。
#
# 它的来历：开发这个项目时本机的文件沙箱只允许写工作区，默认的
# ~/.gradle 写不进去，所以临时把缓存放到工程里。后来它涨到了 2.1G，
# 把磁盘挤爆，导致 Android Studio 写索引失败（报 No space left on device，
# 直接表现是"同步失败"）。现在改成用默认的 ~/.gradle。
#
# 条目留着是为了将来在受限环境里还能临时用它；看到这个目录可以直接删。
.gradle-home/

# ---------- 本地配置 ----------
# 含机器专属的 SDK 路径，不该共享
local.properties

# ---------- 发布签名 ----------
# 密钥和口令**绝不能进仓库**。丢了密钥就永久失去给老用户升级的能力，
# 泄漏了口令别人就能冒名发布"官方升级包"。模板见 keystore.properties.example
keystore.properties
*.jks
*.keystore
# 调试密钥是例外：它是公开的默认口令，工程自带一份方便 clone 后直接构建
!keystore/debug.keystore

# ---------- IDE / 系统 ----------
.idea/
*.iml
.DS_Store
# AI 助手的工作区数据（工作日志、技能等），不是工程的一部分
.workbuddy/

# ---------- 临时文件 ----------
*.log
*.tmp
*~

# ---------- 二进制参考资料（体积大，不入库）----------
*.apk
*.zip

# 本机备份的完整开发历史（见 else/README.md）
*.bundle
