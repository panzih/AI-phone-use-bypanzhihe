# 发布签名配置的模板。
#
# 复制成 keystore.properties 并填上你的值 —— 或者直接跑：
#
#     bash setup_release_keystore.sh
#
# 它会生成密钥、写好这个文件、并把口令打印出来让你抄走。
#
# ⚠️ keystore.properties 和它指向的密钥文件都在 .gitignore 里，
#    永远不要提交，也不要放进任何会被分享出去的目录。
#
# storeFile 的相对路径是相对**项目根目录**的。

storeFile=keystore/release.keystore
storePassword=换成你的口令
keyAlias=zhihe
keyPassword=换成你的口令
